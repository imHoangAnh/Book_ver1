# 📘 BookStation Authentication API Workflow

## 📁 Danh sách các file cần thiết cho API Login & Register

### 🔷 **1. Presentation Layer (PublicApi)**

| File | Đường dẫn | Mô tả |
|------|-----------|-------|
| `AuthController.cs` | `src/BookStation.PublicApi/Controllers/` | Controller xử lý HTTP requests cho login/register |

---

### 🔷 **2. Application Layer (CQRS Commands)**

| File | Đường dẫn | Mô tả |
|------|-----------|-------|
| `RegisterUserCommand.cs` | `src/BookStation.Application/Users/Commands/` | Command + Response cho đăng ký |
| `RegisterUserCommandHandler.cs` | `src/BookStation.Application/Users/Commands/` | Handler xử lý logic đăng ký |
| `RegisterUserCommandValidator.cs` | `src/BookStation.Application/Users/Commands/` | Validator (FluentValidation) cho đăng ký |
| `LoginCommand.cs` | `src/BookStation.Application/Users/Commands/` | Command + Response cho đăng nhập |
| `LoginCommandHandler.cs` | `src/BookStation.Application/Users/Commands/` | Handler xử lý logic đăng nhập |
| `ValidationBehavior.cs` | `src/BookStation.Application/Behaviors/` | Pipeline behavior validate commands |

---

### 🔷 **3. Domain Layer (Entities & Value Objects)**

| File | Đường dẫn | Mô tả |
|------|-----------|-------|
| `User.cs` | `src/BookStation.Domain/Entities/UserAggregate/` | Entity User (Aggregate Root) |
| `Role.cs` | `src/BookStation.Domain/Entities/UserAggregate/` | Entity Role (RBAC) |
| `UserRole.cs` | `src/BookStation.Domain/Entities/UserAggregate/` | Junction table User-Role |
| `Permission.cs` | `src/BookStation.Domain/Entities/UserAggregate/` | Entity Permission |
| `UserEvents.cs` | `src/BookStation.Domain/Entities/UserAggregate/` | Domain Events (UserCreatedEvent, etc.) |
| `Email.cs` | `src/BookStation.Domain/ValueObjects/` | Value Object cho Email |
| `PhoneNumber.cs` | `src/BookStation.Domain/ValueObjects/` | Value Object cho Phone |

---

### 🔷 **4. Domain Repositories (Interfaces)**

| File | Đường dẫn | Mô tả |
|------|-----------|-------|
| `IUserRepository.cs` | `src/BookStation.Domain/Repositories/` | Interface repository User |
| `IRoleRepository.cs` | `src/BookStation.Domain/Repositories/` | Interface repository Role |

---

### 🔷 **5. Infrastructure Layer (Implementations)**

| File | Đường dẫn | Mô tả |
|------|-----------|-------|
| `UserRepository.cs` | `src/BookStation.Infrastructure/Repositories/` | Implementation của IUserRepository |
| `RoleRepository.cs` | `src/BookStation.Infrastructure/Repositories/` | Implementation của IRoleRepository |
| `PasswordHasher.cs` | `src/BookStation.Infrastructure/Services/` | Service hash password (BCrypt) |
| `JwtTokenGenerator.cs` | `src/BookStation.Infrastructure/Authentication/` | Service tạo JWT Token |
| `JwtSettings.cs` | `src/BookStation.Infrastructure/Authentication/` | Settings cho JWT |
| `ConfigureServices.cs` | `src/BookStation.Infrastructure/` | DI registration cho tất cả services |

---

### 🔷 **6. Shared Kernel (Core abstractions)**

| File | Đường dẫn | Mô tả |
|------|-----------|-------|
| `Entity.cs` | `src/BookStation.Core/SharedKernel/` | Base class cho Entities |
| `AggregateRoot.cs` | `src/BookStation.Core/SharedKernel/` | Base class cho Aggregate Roots |
| `ValueObject.cs` | `src/BookStation.Core/SharedKernel/` | Base class cho Value Objects |
| `IUnitOfWork.cs` | `src/BookStation.Core/SharedKernel/` | Interface Unit of Work pattern |

---

## 🔄 Workflow Diagram

### 📌 **Register API Workflow**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           REGISTER API WORKFLOW                             │
│                        POST /api/auth/register                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   HTTP Request   │
│                  │
│  POST /register  │
│  {               │
│   "email": "...",│
│   "password":".."│
│   "fullName":... │
│   "phone": ...   │
│  }               │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        📂 PRESENTATION LAYER                             │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                     AuthController.cs                              │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │ [HttpPost("register")]                                       │  │  │
│  │  │ public async Task<IActionResult> Register(                   │  │  │
│  │  │     [FromBody] RegisterUserCommand command)                  │  │  │
│  │  │ {                                                            │  │  │
│  │  │     var result = await _mediator.Send(command);              │  │  │
│  │  │     return CreatedAtAction(..., result);                     │  │  │
│  │  │ }                                                            │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
         │
         │ _mediator.Send(command)
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        📂 APPLICATION LAYER                              │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                  MediatR Pipeline                                   │ │
│  │  ┌───────────────────────────────────────────────────────────────┐  │ │
│  │  │            ValidationBehavior.cs                              │  │ │
│  │  │  • Nhận RegisterUserCommand                                   │  │ │
│  │  │  • Tìm validators tương ứng                                   │  │ │
│  │  │  • Gọi RegisterUserCommandValidator                           │  │ │
│  │  └─────────────────────────┬─────────────────────────────────────┘  │ │
│  │                            │                                        │ │
│  │                            ▼                                        │ │
│  │  ┌───────────────────────────────────────────────────────────────┐  │ │
│  │  │        RegisterUserCommandValidator.cs                        │  │ │
│  │  │  • Email: NotEmpty, Valid format, Max 255 chars               │  │ │
│  │  │  • Password: Min 8 chars, Uppercase, Lowercase, Number        │  │ │
│  │  │  • FullName: Max 255 chars (optional)                         │  │ │
│  │  │  • Phone: Valid format (optional)                             │  │ │
│  │  └─────────────────────────┬─────────────────────────────────────┘  │ │
│  │                            │ Passed Validation                      │ │
│  │                            ▼                                        │ │
│  │  ┌───────────────────────────────────────────────────────────────┐  │ │
│  │  │         RegisterUserCommandHandler.cs                         │  │ │
│  │  │                                                               │  │ │
│  │  │  async Task<RegisterUserResponse> Handle(                     │  │ │
│  │  │      RegisterUserCommand request, CancellationToken ct)       │  │ │
│  │  │  {                                                            │  │ │
│  │  │     ① Check email unique (IUserRepository)                    │  │ │
│  │  │     ② Create Email Value Object                               │  │ │
│  │  │     ③ Create PhoneNumber Value Object (if provided)           │  │ │
│  │  │     ④ Hash password (IPasswordHasher)                         │  │ │
│  │  │     ⑤ User.Create(...) - Create User Entity                   │  │ │
│  │  │     ⑥ Save to database (IUserRepository.AddAsync)             │  │ │
│  │  │     ⑦ Return RegisterUserResponse                             │  │ │
│  │  │  }                                                            │  │ │
│  │  └───────────────────────────────────────────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
         │
         │ Calls Domain & Infrastructure
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                          📂 DOMAIN LAYER                                 │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        User.cs (Entity)                             │ │
│  │  ┌───────────────────────────────────────────────────────────────┐  │ │
│  │  │ public static User Create(                                    │  │ │
│  │  │     Email email,                                              │  │ │
│  │  │     PasswordHash passwordHash,                                │  │ │
│  │  │     string? fullName,                                         │  │ │
│  │  │     PhoneNumber? phone)                                       │  │ │
│  │  │ {                                                             │  │ │
│  │  │     var user = new User {                                     │  │ │
│  │  │         Email = email,                                        │  │ │
│  │  │         PasswordHash = passwordHash,                          │  │ │
│  │  │         FullName = fullName,                                  │  │ │
│  │  │         Phone = phone,                                        │  │ │
│  │  │         IsVerified = false,                                   │  │ │
│  │  │         Status = EUserStatus.Pending                          │  │ │
│  │  │     };                                                        │  │ │
│  │  │     user.AddDomainEvent(new UserCreatedEvent(user));          │  │ │
│  │  │     return user;                                              │  │ │
│  │  │ }                                                             │  │ │
│  │  └───────────────────────────────────────────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                 Email.cs (Value Object)                             │ │
│  │  • Validate email format với Regex                                  │ │
│  │  • Normalize to lowercase                                           │ │
│  │  • Max 255 characters                                               │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
         │
         │ Save to Database
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      📂 INFRASTRUCTURE LAYER                             │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   PasswordHasher.cs                                 │ │
│  │  • HashPassword(): BCrypt algorithm                                 │ │
│  │                                                                     │ │
│  │                                                                     │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   UserRepository.cs                                 │ │
│  │  • IsEmailUniqueAsync(): Check email tồn tại                        │ │
│  │  • AddAsync(): Thêm user vào context                                │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   WriteDbContext (EF Core)                          │ │
│  │  • SaveChangesAsync() được gọi qua UnitOfWork pattern               │ │
│  │  • Map User entity vào bảng Users                                   │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│  HTTP Response   │
│                  │
│  201 Created     │
│  {               │
│   "userId": 1,   │
│   "email":"...", │
│   "isVerified":  │
│      false       │
│  }               │
└──────────────────┘
```

---

### 📌 **Login API Workflow**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            LOGIN API WORKFLOW                               │
│                         POST /api/auth/login                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   HTTP Request   │
│                  │
│  POST /login     │
│  {               │
│   "email": "...",│
│   "password":".."│
│  }               │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        📂 PRESENTATION LAYER                             │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                     AuthController.cs                              │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │ [HttpPost("login")]                                          │  │  │
│  │  │ [AllowAnonymous]                                             │  │  │
│  │  │ public async Task<IActionResult> Login(                      │  │  │
│  │  │     [FromBody] LoginCommand command)                         │  │  │
│  │  │ {                                                            │  │  │
│  │  │     var result = await _mediator.Send(command);              │  │  │
│  │  │     return Ok(result);                                       │  │  │
│  │  │ }                                                            │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
         │
         │ _mediator.Send(command)
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        📂 APPLICATION LAYER                              │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                  MediatR Pipeline                                   │ │
│  │                                                                     │ │
│  │  ┌───────────────────────────────────────────────────────────────┐  │ │
│  │  │            LoginCommandHandler.cs                             │  │ │
│  │  │                                                               │  │ │
│  │  │  async Task<LoginResponse> Handle(                            │  │ │
│  │  │      LoginCommand request, CancellationToken ct)              │  │ │
│  │  │  {                                                            │  │ │
│  │  │     ① Find user by email (IUserRepository)                    │  │ │
│  │  │     ② Verify password hash (IPasswordHasher)                  │  │ │
│  │  │     ③ Check user status is Active                             │  │ │
│  │  │     ④ Generate JWT token (IJwtTokenGenerator)                 │  │ │
│  │  │     ⑤ Return LoginResponse                                    │  │ │
│  │  │                                                               │  │ │
│  │  │                                                               │  │ │
│  │  │  }                                                            │  │ │
│  │  └───────────────────────────────────────────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
         │
         │ Calls Domain & Infrastructure
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      📂 INFRASTRUCTURE LAYER                             │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   UserRepository.cs                                 │ │
│  │  • GetByEmailAsync(): Tìm user theo email                           │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   RoleRepository.cs                                 │ │
│  │  • GetWithPermissionsAsync(): Lấy role kèm permissions              │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   PasswordHasher.cs                                 │ │
│  │  • VerifyPassword(): So sánh password với hash                      │ │
│  │    - BCrypt.Verify(password, storedHash)                            │ │
│  │                                                                     │ │
│  │                                                                     │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   JwtTokenGenerator.cs                              │ │
│  │                                                                     │ │
│  │  GenerateToken(userId, email, roles, permissions):                  │ │
│  │  ┌───────────────────────────────────────────────────────────────┐  │ │
│  │  │  JWT Claims:                                                  │  │ │
│  │  │  • sub: userId                                                │  │ │
│  │  │  • email: user email                                          │  │ │
│  │  │  • jti: unique token id (Guid)                                │  │ │
│  │  │  • (roles/permissions not in token)                           │  │ │
│  │  │                                                               │  │ │
│  │  │                                                               │  │ │
│  │  │  JWT Configuration:                                           │  │ │
│  │  │  • Algorithm: HMACSHA256                                      │  │ │
│  │  │  • Issuer: from JwtSettings                                   │  │ │
│  │  │  • Audience: from JwtSettings                                 │  │ │
│  │  │  • Expiration: 60 minutes                                     │  │ │
│  │  └───────────────────────────────────────────────────────────────┘  │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│  HTTP Response   │
│                  │
│  200 OK          │
│  {               │
│   "userId": 1,   │
│   "email":"...", │
│   "token":       │
│     "eyJhbG...", │
│   "expiresAt":   │
│     "2024-...",  │
│                  │
│                  │
│                  │
│  }               │
└──────────────────┘
```

---

## 📊 Dependency Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CLEAN ARCHITECTURE LAYERS                            │
└─────────────────────────────────────────────────────────────────────────────┘

     ┌───────────────────────────────────────────────────────────────────┐
     │                     PublicApi (Presentation)                      │
     │                                                                   │
     │                      AuthController.cs                            │
     │                            │                                      │
     │                            │ IMediator                            │
     │                            ▼                                      │
     └───────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
     ┌───────────────────────────────────────────────────────────────────┐
     │                     Application Layer                             │
     │                                                                   │
     │  ┌──────────────────┐   ┌──────────────────┐                      │
     │  │ RegisterUser     │   │ Login            │                      │
     │  │ Command + Handler│   │ Command + Handler│                      │
     │  │ + Validator      │   │                  │                      │
     │  └────────┬─────────┘   └────────┬─────────┘                      │
     │           │                      │                                │
     │           │ IUserRepository      │ IUserRepository                │
     │           │ IPasswordHasher      │ IRoleRepository                │
     │           │                      │ IPasswordHasher                │
     │           │                      │ IJwtTokenGenerator             │
     │           ▼                      ▼                                │
     └───────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
     ┌───────────────────────────────────────────────────────────────────┐
     │                       Domain Layer                                │
     │                                                                   │
     │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │
     │  │   User.cs   │  │   Role.cs   │  │ Email.cs    │               │
     │  │ (Aggregate  │  │ (Entity)    │  │ PhoneNumber │               │
     │  │   Root)     │  │             │  │ (Value Objs)│               │
     │  └─────────────┘  └─────────────┘  └─────────────┘               │
     │                                                                   │
     │  ┌─────────────────────────────────────────────────────────────┐  │
     │  │              Repository Interfaces                          │  │
     │  │  IUserRepository    IRoleRepository    (abstract)           │  │
     │  └─────────────────────────────────────────────────────────────┘  │
     └───────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ implements
                                  │
     ┌───────────────────────────────────────────────────────────────────┐
     │                    Infrastructure Layer                           │
     │                                                                   │
     │  ┌────────────────────────────────────────────────────────────┐   │
     │  │                    Repositories                            │   │
     │  │  UserRepository.cs          RoleRepository.cs              │   │
     │  └────────────────────────────────────────────────────────────┘   │
     │                                                                   │
     │  ┌────────────────────────────────────────────────────────────┐   │
     │  │                     Services                               │   │
     │  │  PasswordHasher.cs         JwtTokenGenerator.cs            │   │
     │  └────────────────────────────────────────────────────────────┘   │
     │                                                                   │
     │  ┌────────────────────────────────────────────────────────────┐   │
     │  │                   Persistence                              │   │
     │  │  WriteDbContext (EF Core)      Database (SQL Server)       │   │
     │  └────────────────────────────────────────────────────────────┘   │
     └───────────────────────────────────────────────────────────────────┘
```

---

## 🔑 Key Design Patterns Used

| Pattern | Implementation | Purpose |
|---------|----------------|---------|
| **CQRS** | Commands (RegisterUserCommand, LoginCommand) | Tách biệt read/write operations |
| **Mediator** | MediatR library | Decouple sender/receiver |
| **Repository** | IUserRepository, IRoleRepository | Abstract data access |
| **Factory Method** | User.Create(), Email.Create() | Encapsulated object creation |
| **Value Object** | Email, PhoneNumber | Immutable business concepts |
| **Aggregate Root** | User entity | Domain consistency boundary |
| **Domain Events** | UserCreatedEvent | Side effects & notifications |
| **Pipeline Behavior** | ValidationBehavior | Cross-cutting concerns |
| **Unit of Work** | WriteDbContext : IUnitOfWork | Transaction management |
| **Dependency Injection** | ConfigureServices.cs | Loose coupling |

---

## 📝 Request/Response Examples

### Register Request
```json
POST /api/auth/register
Content-Type: application/json

{
    "email": "user@example.com",
    "password": "SecurePass123",
    "fullName": "John Doe",
    "phone": "+84901234567"
}
```

### Register Response (201 Created)
```json
{
    "userId": 1,
    "email": "user@example.com",
    "isVerified": false
}
```

### Login Request
```json
POST /api/auth/login
Content-Type: application/json

{
    "email": "user@example.com",
    "password": "SecurePass123"
}
```

### Login Response (200 OK)
```json
{
    "userId": 1,
    "email": "user@example.com",
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expiresAt": "2024-01-19T16:22:18Z"
}
```

---

## ⚠️ Error Responses

| Scenario | Status Code | Response |
|----------|-------------|----------|
| Email already registered | 400 Bad Request | `{"error": "Email 'x@y.com' is already registered."}` |
| Invalid email/password | 401 Unauthorized | `{"error": "Invalid email or password."}` |
| User account not active | 401 Unauthorized | `{"error": "User account is not active."}` |
| Validation failed | 400 Bad Request | FluentValidation errors |

---

## 🏗️ Tổng số files cần thiết: **22 files chính**

1. `AuthController.cs`
2. `RegisterUserCommand.cs`
3. `RegisterUserCommandHandler.cs`
4. `RegisterUserCommandValidator.cs`
5. `LoginCommand.cs`
6. `LoginCommandHandler.cs`
7. `ValidationBehavior.cs`
8. `User.cs`
9. `Role.cs`
10. `UserRole.cs`
11. `Permission.cs`
12. `UserEvents.cs`
13. `Email.cs`
14. `PhoneNumber.cs`
15. `IUserRepository.cs`
16. `IRoleRepository.cs`
17. `UserRepository.cs`
18. `RoleRepository.cs`
19. `PasswordHasher.cs`
20. `JwtTokenGenerator.cs`
21. `JwtSettings.cs`
22. `ConfigureServices.cs`

---

## 📌 **Profile API Workflow**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           PROFILE API WORKFLOW                              │
│                        GET /api/auth/profile                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   HTTP Request   │
│                  │
│  GET /profile    │
│  Headers:        │
│  Authorization:  │
│   Bearer <token> │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        📂 PRESENTATION LAYER                             │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │                     AuthController.cs                              │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │ [HttpGet("profile")]                                         │  │  │
│  │  │ [Authorize]  // Requires valid JWT token                     │  │  │
│  │  │ public IActionResult GetProfile()                            │  │  │
│  │  │ {                                                            │  │  │
│  │  │     ① Extract userId from JWT claims                         │  │  │
│  │  │     ② Extract email from JWT claims                          │  │  │
│  │  │     ③ Extract roles from JWT claims                          │  │  │
│  │  │     ④ Extract permissions from JWT claims                    │  │  │
│  │  │     ⑤ Return user profile from token                         │  │  │
│  │  │ }                                                            │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
         │
         │ ASP.NET Core Authentication Middleware
         │ validates JWT token before reaching controller
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                      📂 AUTHENTICATION MIDDLEWARE                        │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                   JWT Bearer Authentication                         │ │
│  │  • Validate token signature using JwtSettings.Secret               │ │
│  │  • Check token expiration                                           │ │
│  │  • Verify Issuer and Audience                                       │ │
│  │  • Extract claims and populate User.Identity                        │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────┘
         │
         ▼
┌──────────────────┐
│  HTTP Response   │
│                  │
│  200 OK          │
│  {               │
│   "userId": "1", │
│   "email":"...", │
│   "phone":...    │
│                  │
│  }               │
└──────────────────┘
```

### Profile Request
```json
GET /api/auth/profile
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Profile Response (200 OK)
```json
{
    "userId": "1",
    "email": "user@example.com",
    "fullName": "John Doe",
    "phone": null
}
```

### Profile Error Response (401 Unauthorized)
```json
{
    "type": "https://tools.ietf.org/html/rfc7235#section-3.1",
    "title": "Unauthorized",
    "status": 401
}
```

---

## 🧪 Quick Test Commands (cURL)

### Register
```bash
curl -X POST "https://localhost:7000/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"SecurePass123","fullName":"Test User"}'
```

### Login
```bash
curl -X POST "https://localhost:7000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"SecurePass123"}'
```

### Profile (replace TOKEN with actual JWT token)
```bash
curl -X GET "https://localhost:7000/api/auth/profile" \
  -H "Authorization: Bearer TOKEN"
```

---

### 📌 **Update Profile Workflow**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       UPDATE PROFILE API WORKFLOW                           │
│                        PUT /api/auth/profile                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   HTTP Request   │
│                  │
│  PUT /profile    │
│  {               │
│   "fullName":..  │
│   "phone":...    │
│   "bio":...      │
│  }               │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                  UpdateProfileCommandHandler.cs                          │
│                                                                          │
│  async Task<UpdateProfileResponse> Handle(...)                           │
│  {                                                                       │
│     ① Get User by ID (IUserRepository)                                   │
│     ② Create Value Objects (PhoneNumber, Address)                        │
│     ③ User.UpdateProfile(...)                                            │
│     ④ Save to database                                                   │
│     ⑤ Return UpdateProfileResponse                                       │
│  }                                                                       │
└──────────────────────────────────────────────────────────────────────────┘
```

---

### 📌 **Change Password Workflow**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      CHANGE PASSWORD API WORKFLOW                           │
│                     POST /api/auth/change-password                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   HTTP Request   │
│                  │
│ POST /change-pass│
│ {                │
│  "current": ".." │
│  "new": "..."    │
│  "confirm": ".." │
│ }                │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                  ChangePasswordCommandHandler.cs                         │
│                                                                          │
│  async Task<ChangePasswordResponse> Handle(...)                          │
│  {                                                                       │
│     ① Get User by ID                                                     │
│     ② Verify Current Password (IPasswordHasher)                          │
│     ③ Validate New Password (Password.Create)                            │
│     ④ Hash New Password (IPasswordHasher)                                │
│     ⑤ User.ChangePassword(newHash)                                       │
│     ⑥ Save to database                                                   │
│     ⑦ Return Success Response                                            │
│  }                                                                       │
└──────────────────────────────────────────────────────────────────────────┘
```
